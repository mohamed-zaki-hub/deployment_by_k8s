apiVersion: v1
kind: Service
metadata:
  name: go-app
  namespace: go-depi
  labels:
    app: go-app
spec:
  selector:
    app: go-app
  type: NodePort
  ports:
    - name: go-app
      port: 8084
      targetPort: 8084
---
apiVersion: v1
kind: Service
metadata:
  name: mongo-app-service
  namespace: go-depi
  labels:
    app: mongo-app
spec:
  selector:
    app: go-app
  ports:
    - name: mongo-app
      port: 27017
      targetPort: 27017
  type: NodePort
